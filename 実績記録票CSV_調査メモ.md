# 実績記録票CSV（J611）取込不具合 調査メモ

対象症状：**取込エラーは出ないのに、日別データが登録されない。欠席（状況コード3・時間なし）の行だけ登録される。**

---

## 1. なぜこうなっているのか（根本原因）

請求（J121）と実績記録票（J611）で、作り方の土台が違います。

| | 請求 J121 | 実績記録票 J611 |
|---|---|---|
| 基準にしたもの | **実際に取込に成功した見本ファイル**（`J121_202607_final.CSV`） | 取込システムの**エラーリストからの逆算** |
| 保証されていること | 正しい位置に正しい値が入っている | 「**エラーにならない**」ことだけ |

コード内のコメントにもそのまま残っています。

```
// 全項目の位置は取込システムのエラーリスト診断で実測確定済み（2026-07 ErrFile解析）
```

エラーリストは「書式が壊れている項目」しか教えてくれません。
**書式として正しい値を、意味の違う項目に入れてしまった場合はエラーになりません。**
その結果、取込は正常終了するのに中身が登録されない——今の症状と完全に一致します。

つまりこれは「仕様書がないと解けない問題」ではなく、**「見本ファイルがないと解けない問題」**です。J121は見本があったから通り、J611は見本がないから通っていません。

---

## 2. コードを読んで見つかった、疑わしい点（優先順）

`anna_v7.35.html` の `dlJissekiCsv()`（3019行〜）を精査した結果です。v7.84でも同じ構造なら同じ問題が残っています。

### ① 通所日に「サービス提供の状況」＝ `1` を入れている　★最有力

```js
const ST={"通":"1","外":"2","欠":"3","加":"3"};
...
f[11]=e.st;   // 12: サービス提供の状況
```

紙の実績記録票では「サービス提供の状況」欄は、**欠席・施設外支援などの特別な状況のときだけ記入し、通常の通所日は空欄**という運用です。
通所日に `1` を入れると、取込側では「その他の状況コード」と解釈され、**行としては読めるが提供実績としては登録されない**という挙動になり得ます。

そしてこの仮説は、観測事実と過不足なく合います。

- 欠席（`3`）＝ 正しいコードなので登録される ✔
- 通所（`1`）・施設外（`2`）＝ コードの意味が違うので登録されない ✔
- 書式は壊れていないのでエラーは出ない ✔

### ② 施設外日で「状況＝2」と「施設外支援フラグ（項目31）＝1」の二重指定

```js
if(e.st==="2")f[30]="1";   // 31: 施設外支援
```

施設外支援に専用のフラグ項目があるなら、状況欄にも `2` を入れるのは重複です。どちらか一方が正しく、両方入れたことで矛盾扱いになっている可能性があります。

### ③ 開始時間・終了時間の項目番号（16／17番）が未確認

欠席行（時間なし）だけ通るという事実は、**時間を書き込んでいる項目そのものが原因である可能性**も示します。16／17番が本当は別の項目（例：算定時間数、単位数）なら、そこに `0930` を入れた行は不正データとして無視されます。

### ④ 様式種別番号が `1701` で正しいか

```js
f[8]="1701";  // 様式種別番号
```

令和6年改定後の様式番号の並びでは、就労継続支援B型は **16番台（1601）** の可能性があります。1701は就労定着支援に当たる番号かもしれません。

### ⑤ 事業所番号の桁そろえがJ121とJ611で不統一

- J121（取込成功）：`offNo10 = offNo.padEnd(10," ")` … **10桁に空白埋め**
- J611（取込不発）：`f[6]=offNo` … **素のまま**

見本ファイルに合わせた処理がJ611側だけ抜けています。

### ⑥ 最終項目への強制 `"0"`

```js
f[f.length-1]="0"; // 最終項目(未使用の加算欄)に明示的な0を置き…
```

未使用の加算欄に `0` を立てているため、取込側が「加算あり」と誤解する余地があります。

---

## 3. これを確定させる方法（確実な順）

### A. 見本ファイルを手に入れる ← 最短・最確実（推奨）

J121で成功したのと同じやり方です。

1. 簡易入力システムで、**1名分だけ手入力**する（通所2日＋施設外1日＋欠席1日、時間も入れる）
2. その実績記録票を確定し、**送信用データ（またはCSV出力）を作成**する
3. できたファイルを、下記ツールの **②「見本CSVと自作CSVを比較する」** に入れる

これで、状況欄に何が入るのか・時間が何番目の項目なのか・様式種別番号は何番か——**全部が一度に確定します。** 仕様書は要りません。

### B. 取込テスト（プローブ）— 見本が手に入らない場合

ツールの **④「取込テスト用CSV（プローブ）を作る」** で、1日ごとに条件を1つだけ変えたテストファイルを出力し、1回取り込むだけで原因が絞れます。

- 「状況欄を空欄にした日」だけ登録された → ①が原因
- 「時間を入れなかった日」だけ登録された → ③が原因（Bテストで時間の項目位置を探す）
- 「クォートなし／末尾0なしの日」だけ登録された → 出力書式が原因

> テスト後は取り込んだ実績記録票を必ず削除してください。

### C. ヘルプデスクに聞く場合の聞き方

「取り込めません」だけだと一般論の回答になりがちです。**項目番号を挙げて聞く**と即答が得られます。

> 就労継続支援B型の実績記録票を、インタフェース仕様書に沿った取込用CSV（交換情報識別番号 J611）で自作しています。
> 取込エラーは出ないのですが、日別情報レコードのうち欠席（サービス提供の状況＝3）の行だけが登録され、
> 開始時間・終了時間が入っている通所・施設外の行が登録されません。
> 次の4点を教えてください。
> 1. 日別情報レコードの「サービス提供の状況」欄は、通常の通所日も記入するのでしょうか、それとも空欄でしょうか。コード体系（通所・施設外支援・欠席それぞれの値）も教えてください。
> 2. 開始時間・終了時間は日別情報レコードの何項目目で、書式はHHMMの4桁でよいでしょうか。
> 3. 就労継続支援B型の様式種別番号は何番でしょうか（当方は1701で出力しています）。
> 4. 取込がエラーにならずに日別データだけ登録されない場合、原因になり得る項目はどれでしょうか。

---

## 4. 付属ツール：`jisseki_csv_kensa.html`

ブラウザで開くだけで動きます（インターネット接続不要・ファイルは外部に送信されません）。

| 機能 | 内容 |
|---|---|
| ① CSVを読む | Shift-JIS／UTF-8を自動判別し、項目番号つきで中身を表示。Excelで開かずに確認できます |
| ② 見本と比較 | 見本CSVと自作CSVを項目ごとに突き合わせ、違う項目だけを赤く表示 |
| ③ 値さがし | 「`0930` は何項目目に入っているか」を検索。時間の正しい位置を一発で特定 |
| ④ 取込テスト用CSV | 1日ごとに条件を1つだけ変えたプローブファイル＋対応表を出力 |
| ⑤ 変換 | アプリが出したCSVを読み込み、状況欄の空欄化・クォート除去・時間の項目移動などを適用して出し直し（**文字コードはShift-JISのまま維持**、備考の日本語も壊れません） |

⑤があるので、原因が分かった時点で**アプリの改修を待たずに今月分のファイルを作り直せます。**

---

## 5. 請求側の残件：送迎加算（復）

コードを確認しました。加算項目の定義自体は最初から入っており、既定が未チェックなだけです。

```js
{id:12, code:"468590", name:"就継B送迎加算Ⅰ（往）", unit:"21", perType:"送往", enabled:false},
{id:13, code:"468590", name:"就継B送迎加算Ⅰ（復）", unit:"21", perType:"送復", enabled:false},
```

**対応：加算設定で「就継B送迎加算Ⅰ（復）」にチェックを入れてください。** 回数は実績記録の「送迎(復)」チェックから自動集計されるので、手入力は不要です。（同一敷地内の送迎に該当する場合は id14「同一敷地（往）」15単位側を使います）

---

## 6. 次にやること

1. 上記 A（見本ファイル入手）または B（プローブテスト）を実施
2. 結果（見本ファイル、または「どの日が登録されたか」）を共有
3. **v7.84 のHTMLをこのリポジトリに置いていただければ、確定した仕様どおりに本体を修正します**（現在リポジトリにあるのは v7.35 までで、v7.84 のソースがありません）
