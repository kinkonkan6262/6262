# 基本報酬・加算一覧の配信（reward_master.json）の更新手順

お客様のソフト（v7.98 以降）は、起動から数秒後に GitHub 上の
`reward_master.json` を確認します。ソフトに入っている版より新しい版が置いてあると、
加算一覧の画面に「🆕 新しい一覧（版 ○○○○-○○-○○）が販売元から配信されています」と表示され、
お客様が［↩ 最新の一覧に更新］を押したときだけ一覧が入れ替わります。
**自動では書き換わりません**（お客様が書き換えた内容が勝手に消えることはありません）。

見に行く先（2系統・上から順に試します）

1. https://raw.githubusercontent.com/kinkonkan6262/6262/main/reward_master.json
2. https://kinkonkan6262.github.io/6262/reward_master.json

いずれも **main ブランチ** の内容です。更新は main に反映されてはじめてお客様に届きます。

## 報酬改定が出たときの手順

1. `reward_master.json` を開く
2. `rows` の中の `B`（就労継続支援B型）／`A`（A型）／`I`（就労移行支援）を書き換える
   - 1行の形：`{"cat":"区分","name":"名称","unit":"単位数","memo":"備考・要件"}`
   - `cat` は「基本報酬」「体制加算（日額）」「体制加算」「サービス提供時」「月額」「処遇改善」「減算」
   - `cat` が「減算」の行は赤系、「基本報酬」の行は薄い黄色で表示されます
3. `news`（📰 最近の改定情報）に新しい項目を**先頭に**追加する
   - 1件の形：`{"d":"令和○年○月○日","sv":"BAI","t":"見出し","b":"本文","u":"出典URL","un":"出典名"}`
   - `sv` は表示するサービス種別。`B`＝B型／`A`＝A型／`I`＝就労移行（`"BAI"` と書けば3つとも表示）
4. `ver` と `updated` を**新しい日付**（例 `2026-10-01`）にする ← これが更新の合図です
5. main に push する（お客様は次回起動時、または画面の［🔄 配信を確認］で受け取れます）

## 注意

- `ver` は文字列として大小を比べています。必ず `YYYY-MM-DD` の形式で、日付を進めてください。
- JSON の書式（カンマ・かっこ・引用符）が壊れていると配信されません。保存前に JSON として読めるか確認してください。
- インターネットにつながらない環境のお客様には、この `reward_master.json` をそのままお送りし、
  加算一覧の［📥 読み込み］で取り込んでいただけます（v7.98 は配信ファイルの形もそのまま読めます）。
- ソフト本体（HTML）にも同じ内容が入っています。新しい版のソフトを配布するときは、
  `anna_vX.XX.html` 内の `REWARD_MASTER_VER` ／ `REWARD_NEWS` ／ `REWARD_ROWS_B/A/I` も
  同じ内容に更新してください（`reward_master.json` はこの配列から生成しています）。
